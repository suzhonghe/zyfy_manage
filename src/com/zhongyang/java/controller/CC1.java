package com.zhongyang.java.controller;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.List;

import com.alibaba.druid.sql.visitor.functions.Char;

/**
* @author 作者:zhaofq
* @version 创建时间：2016年9月20日 下午2:26:10
* 类说明
*/
public class CC1 extends Thread{
//	  public static void main(String[] args){
//		            int score[] = {67, 69, 75, 32, 89, 90, 99, 100};
//		           /* for(int i = 0;i<score.length-1;i++){//1;循环n-1次(因为每次循环只能拿第一个去比较)，
//		            	for(int j = 0;j<score.length-i-1;j++){//那第一个和剩下进行比较，score.length-i-1是为了提高效率，因为当第一层for循环中i每增加一次，就说第一个数据子和后面的比较熟越少，
//		            		if(score[j]<score[j+1]){
//		            			int temp = score[j];
//		            			score[j] = score[j+1];
//		            			score[j+1] = temp;
//		            		}
//		                }
//		            	System.out.print("第"+(i+1)+"次排序完成:");
//		            	for(int c = 0;c<score.length;c++){
//	                        	System.out.print(score[c]+"\t");
//	                        }
//		            	System.out.println("");
//		            	}*/
//		            int i = score.length-1;  //初始时,最后位置保持不变
//		          /*  while ( i> 0) {
//		                var pos= 0; //每趟开始时,无记录交换
//		                for (var j= 0; j< i; j++)
//		                    if (arr[j]> arr[j+1]) {
//		                        pos= j; //记录交换的位置
//		                        var tmp = arr[j]; arr[j]=arr[j+1];arr[j+1]=tmp;
//		                    }
//		                i= pos; //为下一趟排序作准备
//		             }*/
//		            
//	  }
	  /* static int tempNumber = 0 ; //静态零时变量存放结果值
	     public static void main(String[] args) {
	 
	     Getnumber(5); //传入的数值
	         System.out.println(tempNumber);
	      }
	        public static int Getnumber(int i){
	        	System.out.println(i);
	          if(i<0){   //判断如果传如的数值(1)小于0说明是负数，是不合法的；所以返回-1；        
	               return -1;
	          }
	          if(i==0){  //0!的阶乘等于1;
	               return 1;
	          }
	          if(i==1){  //传如的数值(i)等于1时推出递归；
	               return 1;
	          }else{
	        return tempNumber = i*Getnumber(i-1); //不满足以上条件时进行这些算法；返回个给一个零时全局变量，在main方法中会打印出来值；
	        }
	      }*/

	  public static void main(String[] args) {

		  }
	 
	}